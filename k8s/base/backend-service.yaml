apiVersion: v1
kind: Service
metadata:
  name: smarthunt-backend
  labels:
    app: smarthunt
    component: backend
spec:
  selector:
    app: smarthunt
    component: backend
  ports:
  - name: http
    port: 80
    targetPort: 8000
  type: ClusterIP
